defmodule Sen do
  @moduledoc """
  A minimal Sentry client.
  """

  def capture_event(event) do
  end

  def capture_exception(error) do
  end

  def capture_message(message, level) do
  end

  def add_breadcrumb(crumb) do
  end

  def configure_scope(callback) do
  end

  def last_event_id do
  end

  def start_session do
  end

  def end_session do
  end
end
